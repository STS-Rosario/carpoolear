// Vue 3: Vue.prototype is no longer supported.
// Global properties are now set via app.config.globalProperties in main.js
// For Composition API, import helpers directly:
// import { cssvar, scrollToElement, checkError, getErrors } from '../../utils/helpers'
